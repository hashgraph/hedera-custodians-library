npm run lint
npm run prettier
npm run test

# Verify GPG signature for the staged commits
if ! git verify-commit HEAD &>/dev/null; then
  echo "❌ Previos (HEAD) Commit does not have a valid GPG signature. Please sign your commits."
  exit 1
fi

##! Uncomment for Testing
#exit 1
